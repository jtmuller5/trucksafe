# in-transit

Optional, lower-priority prototype: the same fine-tuned model annotating live coupling video from recorded footage. Not started; this directory exists as a placeholder so the eventual code has an obvious home.
